# M23 GENERATOR SPEC
## Вычисления по таблице

Версия: 1.0 · TECHNICAL CONTRACT · 2026-08-22

| Поле | Значение |
|---|---|
| Навык | M23 |
| `skillId` | `math.data.tables.calculate` |
| `topicId` | `math.data.tables` |
| Генератор | `gen.math.data.tables.calculate` |
| Источник | `generated` |
| Уровни | 1–3 (4–5 не генерируются) |

Краткий код `M23` не является `skillId`. Педагогика — карточка M23 в `CONTENT_MATRIX_MATH.md`.

**PROJECT_DECISION:** `generatorId = gen.math.data.tables.calculate` (уникальный на навык; в матрице черновик `gen.math.data.tables` общий с M22). Таблица только текстом в `question` (без `imageTask`).

Соседи: M22 — чистое чтение без вычисления; M25 — диаграмма; M27 — сюжет «на сколько» без таблицы.

API: `generateM23Task`, `generateM23Series`. Тип: `Task`. L1/L2 — `singleChoice` (4 варианта); L3 — `numberAnswer`.

---

# 1. Назначение

По данным таблицы вычислить сумму, разность, «на сколько» или заполнить пропуск по правилу суммы. Ответ **нельзя** получить одним чтением ячейки (это M22).

---

# 2. Уровни

## L1 — сумма двух ячеек
- Таблица 2×2 или 2×3, числа 1–20.
- Вопрос: сумма двух явно названных ячеек.
- Подтип: `sum_two_cells`.
- Примеры: «Сколько яблок в Пн и Вт вместе?»; «Сумма Маша+Петя в среду»; «Сколько всего груш в двух днях?».

## L2 — типовой расчёт
- Таблица 3×2 или 3×3, числа до 50.
- Подтипы: `row_or_col_sum`, `how_much_more` («на сколько»).
- Примеры: сумма строки; «на сколько больше Вт, чем Пн»; разность двух ячеек.

## L3 — несколько шагов / лишние данные
- Таблица ≥ 3×3, лишняя строка или столбец.
- Подтипы: `multi_step`, `fill_blank_sum`.
- Примеры: сумма двух ячеек минус третья; пропуск в строке при известной сумме строки; сумма нужных при лишнем столбце.

---

# 3. Дистракторы

1. Значение одной из ячеек (чтение без сложения).
2. Сумма/разность соседних ячеек.
3. Арифметическая ошибка ±1…3 или перепутанные слагаемые.
4. Для «на сколько» — сумма вместо разности.

---

# 4. Seed / серии / L4–L5

Только `seededRng`. Fingerprint: таблица + вопросный ключ. Серия 10+10+10. `difficulty` 4|5 → throw.
