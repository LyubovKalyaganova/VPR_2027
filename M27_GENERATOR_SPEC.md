# M27 GENERATOR SPEC
## Задачи на увеличение и уменьшение

Версия: 1.0 · TECHNICAL CONTRACT · 2026-08-22

| Поле | Значение |
|---|---|
| Навык | M27 |
| `skillId` | `math.word_problems.general.comparison` |
| `topicId` | `math.word_problems.general` |
| Генератор | `gen.math.word_problems.comparison` |
| Уровни | 1–3 |

Соседи: M26 — одно действие без акцента сравнения; M25 — диаграмма; M02/M04/M06 — чистые вычисления.

«Во сколько» только при точном делении. L1/L2 — `singleChoice`; L3 — `numberAnswer`.

---

# Уровни

## L1 — «на сколько» с малыми числами
- Числа ≤ 20; разность ≥ 2; вопрос «на сколько больше/меньше».
- Подтип: `how_much_diff`.
- Имена в родительном падеже («У Кати», «У Пети»); согласование числительных.

## L2 — типовая
- Числа до 200; «на сколько» или найти большее/меньшее по разности.
- Подтипы: `how_much_diff`, `find_by_diff`.
- Примеры: типовое сравнение; «на a больше — сколько стало?».

## L3 — «во сколько» рядом с ловушкой «на сколько»
- Вопрос «во сколько раз»; дистрактор = разность.
- Подтип: `how_many_times`.
- Примеры: 36 и 9 → во сколько (4), ловушка «на сколько» (27).

---

# Дистракторы

«На сколько» ↔ «во сколько»; сумма вместо разности; уменьшение вместо увеличения.
