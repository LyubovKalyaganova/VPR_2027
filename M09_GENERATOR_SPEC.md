# M09 — Генератор: перевод и сравнение единиц

Версия: 1.0  
Педагогический источник: `CONTENT_MATRIX_MATH.md`, карточка M09 (заморожена).  
Код приложения матрицей не меняется; этот файл — контракт генератора.

---

# 1. Идентификация

| Поле | Значение |
|---|---|
| Краткий код | `M09` |
| Название | Перевод и сравнение единиц |
| Канонический `skillId` | `math.quantities.units.convert` |
| `topicId` | `math.quantities.units` |
| Генератор | `gen.math.quantities.units` |
| `sourceType` | `generated` |
| Уровни генератора | только **1 / 2 / 3** |

Краткий код `M09` в `skillId` **не** пишется.

---

# 2. Граница навыка

## Относительно соседей

| Сосед | Граница |
|---|---|
| M09 | выбор / перевод / сравнение **единиц вообще** (тип величины уже дан или очевиден) |
| M12 | расчёт **с массой** (кг/г, сложение/разность масс) |
| M13 | расчёт **с длиной** как величиной (не периметр) |
| M14 | единицы **площади** (см²/дм²/м²) |
| M11 | промежутки времени как расчёт, не «перевод единиц вообще» |
| M20 | формула площади фигуры |

Если задание явно про массу/длину/площадь как предметный расчёт — это соседний навык.  
M09 допускает смесь длины/массы/времени **только** как носитель перевода единиц.

## Что умеет ученик

- переводить соседние и типовые единицы (коэффициенты 10, 100, 1000, 60);
- сравнивать две записи в разных единицах;
- выбирать подходящую единицу измерения.

## Что НЕ относится к M09 (L1–L3)

- L4/L5 (ВПР, несколько величин «выбери равные»);
- чистый расчёт массы (M12), длины (M13), площади фигуры (M20);
- периметр (M19);
- скорость (M16).

**PROJECT_DECISION:** L1/L2 — `singleChoice` (4 варианта); L3 — `numberAnswer` для перевода/составных, `singleChoice` для `compare` и `choose_unit`.

---

# 3. Уровни

## L1 — соседние единицы, коэффициент 10 или 100

- одна величина, **без** составных именованных;
- пары только с коэффициентом **10** или **100** (мм↔см, см↔дм, дм↔м, см↔м);
- подтипы: `to_smaller`, `to_larger`, `choose_unit`.

Примеры:

1. Сколько миллиметров в 7 см? → 70  
2. Сколько дециметров в 40 см? → 4  
3. Чем измеряют длину карандаша? → см

## L2 — типовой перевод одной величины

- коэффициент может быть 10 / 100 / **1000** / **60** (м↔км, г↔кг, мин↔ч);
- всё ещё **без** составных;
- подтипы: `to_smaller`, `to_larger`, `compare`.

Примеры:

1. 3 км = ? м → 3000  
2. 2000 г = ? кг → 2  
3. Что больше: 90 мин или 1 ч? → 90 мин

## L3 — составные именованные и сравнение

- составная запись вида «3 м 20 см»;
- сравнение двух записей / перевод составной в одну единицу;
- подтипы: `to_smaller`, `compare`.

Примеры:

1. 2 м 15 см = ? см → 215  
2. 1 кг 200 г = ? г → 1200  
3. Верно ли: 1 м 5 см = 15 см? → нет (ответ числом: 105 см как «сколько см на самом деле», или сравнение)

**Pedagogical note:** типичная ошибка «1 м 5 см = 15 см» должна попадать в дистракторы L3.

L4/L5: `generateM09Task` **бросает ошибку**.

---

# 4. Подтипы и features

| subtype | Уровни | Смысл |
|---|---|---|
| `to_smaller` | L1–L3 | перевод в более мелкую единицу |
| `to_larger` | L1–L2 | перевод в более крупную |
| `compare` | L2–L3 | сравнение двух записей |
| `choose_unit` | L1 | выбор подходящей единицы |

`features`: `coeff_10` / `coeff_100` / `coeff_1000` / `coeff_60`, `length` / `mass` / `time`, `compound`, `wrong_concat_trap`.

---

# 5. generatorParams

```text
quantityFamily   // 'length' | 'mass' | 'time'
fromUnit / toUnit
valueFrom        // числовая часть исходной записи
valueTo          // ответ в целевой единице (базовые мелкие единицы)
compoundParts?   // для составных: [{unit, value}, ...]
subtype
features
seed
```

---

# 6. Дистракторы

- умножил вместо деления / наоборот;
- забыл коэффициент (оставил то же число);
- коэффициент 10 вместо 100 (и наоборот);
- «склеивание» составных: 1 м 5 см → 15;
- соседнее число ±1 разряд.

---

# 7. Self-check

Независимый пересчёт через базовую единицу семьи (мм / г / мин), не через тот же helper сборки вопроса.  
Серия 30 заданий, детерминизм seed, L4/L5 throw.
