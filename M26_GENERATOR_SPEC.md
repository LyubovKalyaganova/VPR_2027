# M26 GENERATOR SPEC
## Текстовая задача в одно действие

Версия: 1.0 · TECHNICAL CONTRACT · 2026-08-22

| Поле | Значение |
|---|---|
| Навык | M26 |
| `skillId` | `math.word_problems.general.one_step` |
| `topicId` | `math.word_problems.general` |
| Генератор | `gen.math.word_problems.one_step` |
| Уровни | 1–3 |

Соседи: M27 — акцент «на/во сколько»; M28 — остаток; M29 — ≥2 действий; M30–M32 — движение.

Одно арифметическое действие: +, −, × или ÷ (нацело). L1/L2 — `singleChoice`; L3 — `numberAnswer`.

---

# Уровни

## L1 — действие почти явно
- Короткая фраза («стало», «купили ещё», «раздали поровну»).
- Числа малы (до 20 / таблица умножения).
- Подтипы: `add`, `sub`, `mul`, `div`.
- Примеры: «Было 7, дали ещё 5. Сколько стало?»; «12 конфет раздали по 3. Сколько получили?».

## L2 — типовая школьная
- Обычный сюжет, действие не названо глаголом-операцией напрямую.
- Числа до 100 / таблица ×.
- Подтипы те же.
- Примеры: купили тетради; осталось после расхода; цена×количество.

## L3 — лишнее число в тексте
- Одно нужное действие; в тексте есть лишнее данное.
- Подтип: `extra_number`.
- Примеры: возраст брата не нужен; день недели не нужен.

---

# Дистракторы

Неверное действие над теми же числами; использование лишнего числа; ответ = одно из данных.
