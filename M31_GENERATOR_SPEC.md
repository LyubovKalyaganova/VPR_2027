# M31 — Генератор: нахождение времени в движении

Версия: 1.0  
Педагогический источник: `CONTENT_MATRIX_MATH.md`, карточка M31.

| Поле | Значение |
|---|---|
| Краткий код | `M31` |
| `skillId` | `math.word_problems.motion.time` |
| `topicId` | `math.word_problems.motion` |
| Генератор | `gen.math.word_problems.motion.time` |
| Уровни | **1 / 2 / 3** |

Формула: **t = s : v**, деление нацело в часах (или целые минуты после перевода).

## Границы

| Сосед | Граница |
|---|---|
| M11 | бытовая длительность без движения |
| M06 | деление как техника |
| M30/M32 | расстояние / скорость |
| M16 | единицы скорости без сюжета |

## Уровни

- **L1:** s и v целые, t целое число часов, без перевода.
- **L2:** типовая (шире диапазон; ответ в часах, без перевода).
- **L3:** перевод единиц (ответ в минутах при данных в км и км/ч, или данные с минутами/метрами).

Взаимное исключение: L1/L2 без `needs_unit_convert`; L3 с ним.

L4/L5 — throw.

## Подтипы

`exact_hours` (L1), `typical_hours` (L2), `answer_minutes` / `mixed_units` (L3).

## Дистракторы

`s × v`; `s − v`; ответ расстоянием; ошибка перевода ×60 / ÷60.

L1/L2: `singleChoice`; L3: `numberAnswer`.

## Примеры

| L | Пример |
|---|---|
| 1 | 120 км, 40 км/ч → 3 ч |
| 2 | 210 км, 70 км/ч → 3 ч |
| 3 | 90 км, 60 км/ч → 90 мин |

`PROJECT_DECISION`: отдельный `generatorId` на skill (не общий семейный id матрицы).
