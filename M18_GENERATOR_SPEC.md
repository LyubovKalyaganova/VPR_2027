# M18 — Генератор: фигура на клетчатой бумаге (чтение)

Версия: 1.0  
Источник: карточка M18 в `CONTENT_MATRIX_MATH.md`.

---

# 1. Идентификация

| Поле | Значение |
|---|---|
| Краткий код | `M18` |
| `skillId` | `math.geometry.grid.read` |
| `topicId` | `math.geometry.grid` |
| Генератор | `gen.math.geometry.grid` |
| Уровни | **1 / 2 / 3** |

---

# 2. Граница

| Сосед | Граница |
|---|---|
| M18 | сколько клеток / длина стороны **в клетках** / масштаб «клетка = 1 см» как чтение |
| M19 | вопрос «найди периметр» → M19 |
| M20 | вопрос «найди площадь» → M20 |

## PROJECT_DECISION

Чертёж передаётся **текстом** (описание сторон в клетках), без `imageTask` в L1–L3.

---

# 3. Уровни

## L1 — горизонтальный отрезок

- длина отрезка в клетках по горизонтали;
- без составных контуров.

## L2 — прямоугольник на сетке

- ширина и высота в клетках;
- вопрос: сторона в клетках **или** сколько клеток внутри (подсчёт клеток как чтение, не формула площади как цель M20 — допускаем «сколько клеток», без см²).

## L3 — составной контур

- L-образная или составная фигура;
- подсчёт клеток **или** длина ломаной стороны в клетках;
- **не** спрашивать периметр/площадь в см.

---

# 4. Подтипы

| subtype | Уровень |
|---|---|
| `horizontal_segment` | L1 |
| `rectangle_cells` | L2 |
| `composite_cells` | L3 |

---

# 5. Примеры

| L | Пример |
|---|---|
| L1 | «Отрезок занимает 6 клеток вправо. Длина в клетках?» → 6 |
| L1 | «Клетка = 1 см. Отрезок 4 клетки. Длина в см?» → 4 |
| L2 | «Прямоугольник 5×3 клетки. Сколько клеток внутри?» → 15 |
| L2 | «Нижняя сторона прямоугольника — 7 клеток» → 7 |
| L3 | «Буква Г: 4 клетки вниз и 3 вправо без угла дважды» → клетки составной |
| L3 | «Составной контур: … Сколько клеток?» |

---

# 6. PROJECT_DECISION

1. `generatorId = gen.math.geometry.grid`.
2. Текст вместо картинки.
3. Вопросы только про клетки / сторону в клетках / см при клетке=1 см; без P и S.
