# M24 GENERATOR SPEC
## Чтение диаграммы

Версия: 1.0 · TECHNICAL CONTRACT · 2026-08-22

| Поле | Значение |
|---|---|
| Навык | M24 |
| `skillId` | `math.data.charts.read` |
| `topicId` | `math.data.charts` |
| Генератор | `gen.math.data.charts.read` |
| Уровни | 1–3 |

**PROJECT_DECISION:** диаграмма только как **текст** (категории, высоты в делениях, цена деления шкалы). Изображений нет. `generatorId` уникален (`…charts.read`; матрица: общий `gen.math.data.charts`).

Соседи: M25 — «на сколько / сумма / во сколько»; M22/M23 — таблицы.

L1/L2 — `singleChoice`; L3 — `numberAnswer`.

---

# Уровни

## L1 — целые деления шкалы
- Цена деления 1, 2, 5 или 10; высота кратна делению.
- Вопрос: значение одного столбца **или** кто больше/меньше (без «на сколько»).
- Подтипы: `read_bar`, `who_more`.
- Примеры: «Сколько у Ани?»; «У кого столбец выше?»; «Чему равно значение Бори?».

## L2 — типовая диаграмма
- 3–4 категории, цена деления 2/5/10, значения на делениях.
- Подтипы: `read_bar`, `scale_step` (цена деления).
- Примеры: прочитать столбец; «какова цена деления?».

## L3 — между делениями
- Значение между двумя отметками шкалы (например шкала 10, значение 25).
- Подтип: `between_marks`.
- Примеры: столбец между 20 и 30 ближе к середине → 25; «сколько у Кати, если столбец посередине между 40 и 50?».

---

# Дистракторы

Число делений без умножения на цену; соседний столбец; соседняя отметка шкалы; для L3 — ближайшая целая отметка.

Не спрашивать «на сколько больше» (M25).
