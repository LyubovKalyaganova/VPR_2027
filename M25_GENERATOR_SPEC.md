# M25 GENERATOR SPEC
## Сравнение данных диаграммы

Версия: 1.0 · TECHNICAL CONTRACT · 2026-08-22

| Поле | Значение |
|---|---|
| Навык | M25 |
| `skillId` | `math.data.charts.compare` |
| `topicId` | `math.data.charts` |
| Генератор | `gen.math.data.charts.compare` |
| Уровни | 1–3 |

**PROJECT_DECISION:** диаграмма текстом (как M24). `generatorId = gen.math.data.charts.compare`. «Во сколько» только при целочисленном делении.

Соседи: M24 — чтение без сравнения-расчёта; M23 — таблица; M27 — сюжет без диаграммы.

L1/L2 — `singleChoice`; L3 — `numberAnswer`.

---

# Уровни

## L1 — два столбца
- Подтипы: `who_greater`, `how_much_more`.
- Значения **только** на SVG; в тексте вопроса значений нет.

## L2 — три категории
- Подтипы: `how_much_more`, `sum_two`.
- Сравнение / сумма двух считываемых столбцов.

## L3 — ≥3 столбца, вычисление после чтения
- Подтипы: `sum_with_distractor`, `times_exact`, `how_much_more`.
- «Во сколько» только при целочисленном делении.
- Без meta-подсказок («остальные не нужны»).
- Без утечки значений в question.

---

# Дистракторы

«Во сколько» как разность и наоборот; сумма всех столбцов включая лишний; соседняя разность.
