# M17 — Генератор: распознавание геометрических фигур

Версия: 1.0  
Педагогический источник: `CONTENT_MATRIX_MATH.md`, карточка M17 (заморожена).

---

# 1. Идентификация

| Поле | Значение |
|---|---|
| Краткий код | `M17` |
| Название | Распознавание геометрических фигур |
| Канонический `skillId` | `math.geometry.figures.identify` |
| `topicId` | `math.geometry.figures` |
| Генератор | `gen.math.geometry.figures` |
| `sourceType` | `generated` |
| Уровни | только **1 / 2 / 3** |

---

# 2. Граница навыка

| Сосед | Граница |
|---|---|
| M17 | назвать / узнать фигуру по **текстовому** описанию |
| M18 | клетки сетки — не название фигуры |
| M19 / M20 | периметр / площадь — **не** M17 |
| M21 | симметрия — отдельный навык |

## PROJECT_DECISION (текст вместо картинки)

Матрица допускает `imageTask`. Для программного генератора L1–L3 фигуры задаются **только текстом** (описание свойств / положения). Картинки не генерируются.

---

# 3. Уровни

## L1 — эталонная фигура

- однозначное каноническое описание (квадрат, отрезок, луч, круг и т.п.);
- ответ — название фигуры;
- без «непривычного» положения.

## L2 — выбор из 4

- описание с типичной путаницей (квадрат vs прямоугольник, луч vs отрезок, круг vs окружность);
- `singleChoice` из 4 названий.

## L3 — непривычное положение

- фигура описана повёрнутой / «стоит на углу» / стороны не горизонтальны;
- ученик должен узнать ту же фигуру по свойствам, не по «привычной картинке».

L4/L5 бросают ошибку.

---

# 4. Подтипы

| subtype | Уровни |
|---|---|
| `canonical_name` | L1 |
| `choose_from_four` | L2 |
| `unusual_orientation` | L3 |

Фигуры 4 класса: отрезок, прямая, луч, угол, треугольник, прямоугольник, квадрат, многоугольник, окружность, круг.

---

# 5. Дистракторы

Типичные ошибки карточки: квадрат≠прямоугольник в быту; луч=отрезок; круг=окружность; угол=ломаная.

---

# 6. Примеры

| L | Пример |
|---|---|
| L1 | «4 равные стороны и 4 прямых угла» → квадрат |
| L1 | «Две точки и часть прямой между ними» → отрезок |
| L2 | «Есть начало, нет конца» → луч (из 4) |
| L2 | «Граница круга» → окружность |
| L3 | «Квадрат повёрнут и стоит на одной вершине» → квадрат |
| L3 | «Прямоугольник наклонён, стороны не вдоль клеток» → прямоугольник |

---

# 7. PROJECT_DECISION

1. `generatorId = gen.math.geometry.figures` (в матрице черновик «обычно статика» — генератор текстовый).
2. `topicId = math.geometry.figures`.
3. Без периметра/площади в вопросе.
