# M28 GENERATOR SPEC
## Задачи с остатком

Версия: 1.0 · TECHNICAL CONTRACT · 2026-08-22

| Поле | Значение |
|---|---|
| Навык | M28 |
| `skillId` | `math.word_problems.general.remainder` |
| `topicId` | `math.word_problems.general` |
| Генератор | `gen.math.word_problems.remainder` |
| Уровни | 1–3 |

Опора на M07: всегда `0 < remainder < divisor`. Не M06 (остаток 0 запрещён).

Соседи: M07 — чистое деление с остатком; M26 — одно действие без акцента остатка; M29 — многошаговые.

L1/L2 — `singleChoice`; L3 — `numberAnswer`.

---

# Уровни

## L1 — малые числа, вопрос про остаток
- Делимое ≤ 30, делитель 2–9; спросить остаток.
- Подтип: `ask_remainder`.
- Примеры: «17 яблок в коробки по 5. Сколько останется?».

## L2 — типовая
- Делимое до 100; вопрос про остаток или полные группы.
- Подтипы: `ask_remainder`, `ask_groups`.
- Примеры: сколько полных коробок; сколько останется.

## L3 — вопрос про число коробок (остаток не явно)
- Спросить число полных групп; остаток есть, но не в вопросе.
- Подтип: `ask_boxes_only`.
- Примеры: «Сколько коробок заполнится полностью?».

---

# Дистракторы

Частное вместо остатка и наоборот; remainder ≥ divisor; округление вверх.
