# M14 — Генератор: площадь как величина (единицы)

Версия: 1.0  
Источник: карточка M14.

| `skillId` | `math.quantities.area.convert` |
| `topicId` | `math.quantities.area` |
| Генератор | `gen.math.quantities.area` |

# Границы

Только единицы площади см² / дм² / м²: выбор, перевод, сравнение.  
**Не** S = a × b (это M20). Не периметр (M19).

# Уровни

- **L1:** выбор единицы площади **или** простое соотношение (1 м² = 100 дм²; 1 м² = 10 000 см²). Подтипы: `choose_unit`, `relation`.
- **L2:** перевод единиц (в обе стороны), в т.ч. ×100 и ×10 000.
- **L3:** составной перевод / сравнение после приведения к одной единице. Без формулы площади фигуры (M20).

**PROJECT_DECISION:** коэффициенты 1 м² = 100 дм² = 10 000 см²; 1 дм² = 100 см².

L1/L2 — `singleChoice`; L3 — `numberAnswer` (перевод) или `singleChoice` (сравнение).

# Подтипы

`choose_unit` | `relation` | `convert` | `compare`

# Дистракторы

см вместо см²; коэффициент 10 вместо 100; площадь как периметр.
