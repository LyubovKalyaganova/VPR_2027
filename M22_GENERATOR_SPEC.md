# M22 — Генератор: чтение таблицы

Версия: 1.0  
Источник: карточка M22.

---

# 1. Идентификация

| Поле | Значение |
|---|---|
| Краткий код | `M22` |
| `skillId` | `math.data.tables.read` |
| `topicId` | `math.data.tables` |
| Генератор | `gen.math.data.tables.read` |
| Уровни | **1 / 2 / 3** |

---

# 2. PROJECT_DECISION

В матрице черновик `gen.math.data.tables` общий для M22/M23.  
Для разделения навыков генератор чтения использует **`gen.math.data.tables.read`**. Вычисления (сумма/разность) — M23, другой генератор.

Таблица вшивается **текстом** в `question` (ASCII / псевдотаблица).

---

# 3. Граница

| Сосед | Граница |
|---|---|
| M22 | прочитать ячейку; max/min по строке/столбцу; сопоставить строку и столбец |
| M23 | сумма, разность, «на сколько больше» — **запрещено** |

---

# 4. Уровни

## L1 — таблица 2×2, прямой вопрос

- ровно 2 строки данных × 2 столбца значений (плюс заголовки);
- вопрос дословно по заголовкам.

## L2 — типовая школьная таблица

- 3×3 или 3×4 данных;
- прямое чтение или max/min.

## L3 — лишние строки, вопрос не дословный

- ≥4 строк данных;
- формулировка не копирует заголовок один в один («в какой день было холоднее» при столбце «температура»).

---

# 5. Подтипы

| subtype | Уровень |
|---|---|
| `direct_2x2` | L1 |
| `typical_table` | L2 |
| `indirect_extra_rows` | L3 |

---

# 6. Дистракторы

Соседняя ячейка; значение из другой строки/столбца; заголовок вместо числа.

---

# 7. Примеры

| L | Пример |
|---|---|
| L1 | 2×2: «Сколько яблок у Ани?» |
| L1 | «Кто собрал 5 грибов?» |
| L2 | Расписание / очки за 3 дня — max |
| L2 | Прямое чтение ячейки 3×3 |
| L3 | 5 строк, «в какой день было меньше всего осадков?» |
| L3 | Вопрос синонимом к заголовку |

---

# 8. PROJECT_DECISION

1. `generatorId = gen.math.data.tables.read`.
2. L1/L2 `singleChoice`, L3 `singleChoice` или `numberAnswer` при числовом ответе без арифметики.
3. Никаких сумм/разностей в правильном ответе.
