# M32 — Генератор: нахождение скорости в движении

Версия: 1.0  
Педагогический источник: `CONTENT_MATRIX_MATH.md`, карточка M32.

| Поле | Значение |
|---|---|
| Краткий код | `M32` |
| `skillId` | `math.word_problems.motion.speed` |
| `topicId` | `math.word_problems.motion` |
| Генератор | `gen.math.word_problems.motion.speed` |
| Уровни | **1 / 2 / 3** |

Формула: **v = s : t**, ответ в км/ч.

## Границы

| Сосед | Граница |
|---|---|
| M16 | сравнить единицы скорости без сюжета «проехал/ехал» |
| M30/M31 | расстояние / время |
| M06 | деление как техника |

## Уровни

- **L1:** целые s (км), целые t (ч), деление нацело.
- **L2:** типовая (шире числа, всё ещё целые часы без перевода).
- **L3:** перевод времени (минуты → часы) перед делением.

L4/L5 — throw.

## Подтипы

`exact_division` (L1), `typical` (L2), `time_convert` (L3).

## Дистракторы

`s × t`; `s − t`; ответ расстоянием; деление «сырых» минут без перевода.

L1/L2: `singleChoice`; L3: `numberAnswer`.

## Примеры

| L | Пример |
|---|---|
| 1 | 120 км за 3 ч → 40 км/ч |
| 2 | 280 км за 4 ч → 70 км/ч |
| 3 | 90 км за 90 мин → 60 км/ч |

`PROJECT_DECISION`: отдельный `generatorId` на skill.
