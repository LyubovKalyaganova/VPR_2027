# M33 — Генератор: закономерности и последовательности

Версия: 1.0  
Источник: `CONTENT_MATRIX_MATH.md`, карточка M33.

| Поле | Значение |
|---|---|
| Краткий код | `M33` |
| `skillId` | `math.logic.problems.sequence` |
| `topicId` | `math.logic.problems` |
| Генератор | `gen.math.logic.sequence` |
| Уровни | **1 / 2 / 3** |

## Границы

| Сосед | Граница |
|---|---|
| M34 | истинность утверждений, не продолжение ряда |
| M35 | соответствие/исключение по условиям, не ряд |
| M03–M05 | арифметика как техника |

## Уровни

- **L1:** постоянная разность (+2, +5, +10 и т.п.), 4–5 членов, найти следующий.
- **L2:** типовой ряд — умножение на константу или большая разность.
- **L3:** чередование двух правил **или** «выбери лишний».

L4/L5 — throw.

## Подтипы

`add_const` (L1), `mul_const` / `add_large` (L2), `two_rule_alternate` / `odd_one_out` (L3).

## Дистракторы

продолжил только последнее приращение; сложил вместо умножения; ± шаг.

L1/L2: `singleChoice`; L3: `singleChoice` (в т.ч. odd_one_out).

## Примеры

| L | Пример |
|---|---|
| 1 | 3, 5, 7, 9, ? → 11 |
| 2 | 2, 6, 18, 54, ? → 162 |
| 3 | 2, 4, 8, 10, 20, ? (×2, +2) → 22 |

`PROJECT_DECISION`: ответ — число (следующий член или лишний элемент как число из ряда).
